{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun build ./src/index.ts --compile --outfile server",
  "outputDirectory": ".",
  "functions": {
    "server": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [{ "src": "/(.*)", "middleware": "server" }]
}
